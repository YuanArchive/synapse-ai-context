# 🚀 Synapse 빠른 시작 가이드

## 완전 초보자를 위한 5분 설치

이 가이드는 **프로그래밍 경험이 전혀 없는 분**도 Synapse를 설치하고 사용할 수 있도록 작성되었습니다.

---

## 📋 준비물

### 필수
- Windows 10/11 컴퓨터
- 인터넷 연결

### 선택 (자동 설치됨)
- Python (자동 설치)
- Git (수동 설치 필요)

---

## 🎯 Step 1: Git 설치 (한 번만)

### Git이 이미 있는지 확인

1. **Windows 검색**에서 `cmd` 입력
2. **명령 프롬프트** 실행
3. 다음 명령어 입력:
   ```cmd
   git --version
   ```

**결과:**
- ✅ 버전이 나오면 → **Step 2로 이동**
- ❌ 에러가 나면 → 아래 설치 진행

### Git 설치하기

1. [Git 공식 사이트](https://git-scm.com/download/win) 방문
2. **"Click here to download"** 클릭
3. 다운로드된 파일 실행
4. **계속 Next 클릭** (기본 설정 사용)
5. **Install** 클릭
6. 완료 후 **명령 프롬프트 닫고 다시 열기**

---

## 🎯 Step 2: Synapse 다운로드

### 2-1. 폴더 준비

1. **원하는 위치**에 폴더 생성 (예: `C:\Projects`)
2. 해당 폴더에서 **주소창 클릭** → `cmd` 입력 → Enter

### 2-2. Synapse 클론

명령 프롬프트에 다음 입력:
```cmd
git clone https://github.com/YuanArchive/synapse-ai-context.git
cd synapse-ai-context
```

---

## 🎯 Step 3: 원클릭 설치

### 3-1. 설치 스크립트 실행

1. 탐색기에서 `synapse-ai-context\scripts` 폴더 열기
2. **`setup.bat`** 파일을 **더블클릭**

![setup.bat 위치](docs/images/setup-location.png)

### 3-2. 설치 진행

**화면에 나타나는 것들:**

```
=====================================================
  Synapse AI Context Tool - 원클릭 설치
=====================================================

ℹ️  Python 3.12가 설치되지 않았습니다.
Chocolatey를 설치하시겠습니까? (Y/N)
```

**➡️ `Y` 입력 후 Enter**

**다음에 이런 질문이 나올 수 있습니다:**
```
기존 가상환경을 삭제하고 재생성하시겠습니까? (Y/N)
```

**➡️ 처음 설치라면 그냥 Enter (또는 `N`)**

### 3-3. 설치 완료 대기

**설치 중 화면:**
- "Chocolatey 설치 중..."
- "Python 3.12 설치 중..." (시간 걸림)
- "가상환경 생성 중..."
- "Synapse 설치 중..." (시간 걸림)
- "VS Code 설정 구성 중..."

**⏱️ 약 5-10분 소요 (인터넷 속도에 따라 다름)**

### 3-4. 설치 완료 확인

다음 메시지가 나오면 성공:

```
╔══════════════════════════════════════════════════════╗
║  ✅ Synapse 설치가 완료되었습니다!                    ║
╚══════════════════════════════════════════════════════╝
```

**아무 키나 눌러 닫기**

---

## 🎯 Step 4: 첫 실행

### 4-1. VS Code로 프로젝트 열기

1. **VS Code** 실행
2. **File → Open Folder**
3. `synapse-ai-context` 폴더 선택
4. **"신뢰하시겠습니까?"** → **Yes, I trust**

### 4-2. 터미널 열기

VS Code에서:
- **Ctrl + `** (백틱, 숫자 1 왼쪽 키)

또는:
- **Terminal → New Terminal**

### 4-3. 첫 명령어 실행

터미널에 입력:
```bash
synapse --help
```

**성공 시 출력:**
```
Usage: synapse [OPTIONS] COMMAND [ARGS]...

  Synapse AI Context Tool

Options:
  --help  Show this message and exit.

Commands:
  analyze  Analyze project and build index
  init     Initialize Synapse in project
  search   Search codebase
  ...
```

---

## 🎯 Step 5: 프로젝트 초기화

### 5-1. Synapse 초기화

```bash
synapse init
```

**결과:**
- `.synapse` 폴더 생성
- 설정 파일 생성

### 5-2. 프로젝트 분석

```bash
synapse analyze .
```

**화면:**
```
🔍 Analyzing project...
📊 Found 24 files
🧠 Building embeddings...
✅ Analysis complete!
```

**⏱️ 프로젝트 크기에 따라 1-5분 소요**

---

## 🎯 Step 6: 첫 검색

```bash
synapse search "로그인"
```

**결과:**
```
🔍 Search Results:

1. src/auth/login.py:15
   def handle_login(username, password):
       # 로그인 처리 함수
       ...

2. tests/test_auth.py:42
   def test_login_success():
       # 로그인 성공 테스트
       ...
```

---

## 🎉 완료!

이제 Synapse를 사용할 준비가 완료되었습니다!

### 다음 단계

📖 **전체 매뉴얼**: [`docs/MANUAL_KO.md`](MANUAL_KO.md)  
🤖 **AI 설정**: [`.agent/AI_RULES_KO.md`](.agent/AI_RULES_KO.md)  
💡 **명령어 모음**: [`README_KO.md`](../README_KO.md#-명령어)

---

## ❓ 문제 해결

### ❌ "synapse를 찾을 수 없습니다"

**해결책 1: 가상환경 활성화**
```bash
.\.venv\Scripts\Activate.ps1
```

**해결책 2: python -m 사용**
```bash
python -m synapse --help
```

### ❌ "Fatal error in launcher"

**해결책:**
모든 명령어에 `python -m` 추가:
```bash
python -m synapse init
python -m synapse analyze .
```

### ❌ "Chocolatey 설치 실패"

**수동 Python 설치:**
1. [Python 3.12.12](https://www.python.org/downloads/) 다운로드
2. **"Add Python to PATH"** 체크
3. Install
4. `setup.bat` 다시 실행 시 **"Chocolatey 설치 건너뛰기" 선택**

### ❌ "C++ 컴파일러 필요"

**해결책:**
1. [Visual Studio Build Tools](https://visualstudio.microsoft.com/visual-cpp-build-tools/) 다운로드
2. **"Desktop development with C++"** 체크
3. Install
4. 컴퓨터 재시작
5. `setup.bat` 다시 실행

---

## 💬 도움이 필요하신가요?

- 🐛 [GitHub Issues](https://github.com/YuanArchive/synapse-ai-context/issues)
- 📧 이메일: [프로젝트 관리자]
- 💬 Discord: [커뮤니티 링크]

---

**Happy Coding! 🚀**
